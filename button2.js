import * as tableChangeIndex2 from './tableChangeIndex2';
import * as tableChangeIndex from './tableChangeIndex';
/* @type {string} */
const id = `button2`;

/* @type {string} */
const type = `widget`;

/* @type {string} */
const subtype = `ButtonWidget`;

/* @type {string} */
const container = ``;

/* @type {string} */
const createdAt = `2020-07-20T16:26:45.317Z`;

/* @type {string} */
const updatedAt = `2020-08-26T15:53:39.939Z`;

/** @typedef {Object} Position2
 * @property {Number} col
 * @property {String} container
 * @property {Number} height
 * @property {Number} row
 * @property {String} subcontainer
 * @property {Number} tabNum
 * @property {Number} width
 */
class Position2 {
  container = ``;
  subcontainer = ``;
  row = 11;
  col = 6;
  height = 1;
  width = 2;
  tabNum = 0;
}

class Template {
  valueToCopy = ``;
  color = `#3c92dc`;
  internalUrlHashParams = ``;
  exportFileType = `csv`;
  buttonType = `action`;
  url = ``;
  newWindow = false;
  value = `Then Me`;
  exportQuery = ``;
  action = `tableChangeIndex2`;
  internalUrlQuery = ``;
  internalUrlPath = ``;
  exportFileName = ``;
  loading = ``;
  disabled = ``;
}
